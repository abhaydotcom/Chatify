<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>Your OTP</title>
    <style>
      body { margin:0; padding:0; font-family: Arial, sans-serif; background:#f5f7fb; color:#111827; }
      .card { max-width:520px; margin:28px auto; background:#fff; border-radius:8px; padding:24px; box-shadow:0 6px 18px rgba(6,18,40,0.08); }
      .logo { height:40px; margin-bottom:12px; }
      h1 { font-size:18px; margin:0 0 8px; }
      p { margin:10px 0; color:#4b5563; font-size:15px; line-height:1.5; }
      .otp { display:inline-block; background:#0b74de; color:#fff; padding:12px 18px; font-size:20px; border-radius:6px; letter-spacing:4px; font-weight:700; margin:14px 0; }
      .small { color:#9aa4b2; font-size:13px; }
      .footer { margin-top:18px; font-size:13px; color:#9aa4b2; border-top:1px solid #eef2f6; padding-top:12px; }
      @media (max-width:420px) { .card { padding:16px; } .otp { font-size:18px; padding:10px 14px; } }
    </style>
  </head>
  <body>
    <div class="card" role="article" aria-label="One-time passcode">
      {{#if logoUrl}}
        <img src="{{logoUrl}}" alt="{{appName}} logo" class="logo">
      {{/if}}

      

      <p>Use the one-time passcode (OTP) below to complete your action on Mern-auth .</p>

      <div class="otp" aria-hidden="false" role="text">{{otp}}</div>

      <p class="small">This OTP will expire in  10 minute{{#unless isSingular}}s{{/unless}}. Do not share it with anyone.</p>

      <p class="small">If you did not request this, please  ignore it.</p>

      <div class="footer">
        © {{currentYear}} {{appName}}. All rights reserved.
      </div>
    </div>
  </body>
</html>
